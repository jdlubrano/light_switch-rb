source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in light_switch.gemspec.
gemspec

gem "debug", ">= 1.0.0"
gem "sprockets-rails"
gem "sqlite3"
